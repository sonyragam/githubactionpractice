name: event
on: 
  pull_request: 
    branches: 
    - main
    types: 
    - reopened
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: onestep
        run: echo " Event Triggered by ${{github.actor}} "
